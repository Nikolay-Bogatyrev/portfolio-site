# Инструкция по работе с Git и публикации на GitHub Pages

## 📋 Содержание
1. [Установка и настройка Git](#установка-и-настройка-git)
2. [Создание репозитория](#создание-репозитория)
3. [Первый коммит](#первый-коммит)
4. [Публикация на GitHub Pages](#публикация-на-github-pages)
5. [Обновление сайта](#обновление-сайта)
6. [Продвинутые возможности](#продвинутые-возможности)
7. [Troubleshooting](#troubleshooting)

---

## 🔧 Установка и настройка Git

### Установка Git

**Windows:**
1. Скачайте Git с [git-scm.com](https://git-scm.com/download/win)
2. Запустите установщик
3. Используйте настройки по умолчанию

**macOS:**
```bash
# Через Homebrew
brew install git

# Или скачайте с git-scm.com
```

**Linux:**
```bash
# Ubuntu/Debian
sudo apt-get install git

# Fedora
sudo dnf install git
```

### Первичная настройка Git

Откройте терминал в Cursor (Ctrl/Cmd + `) и выполните:

```bash
# Настройте имя пользователя
git config --global user.name "Ваше Имя"

# Настройте email (используйте тот же, что и на GitHub)
git config --global user.email "your.email@example.com"

# Проверьте настройки
git config --list
```

---

## 📁 Создание репозитория

### Вариант 1: Специальный репозиторий для GitHub Pages (рекомендуется)

Этот вариант даст вам короткий URL: `https://username.github.io`

1. **Создайте репозиторий на GitHub:**
   - Зайдите на [github.com](https://github.com)
   - Нажмите `+` → `New repository`
   - Назовите репозиторий **точно так**: `username.github.io` (замените `username` на ваш GitHub username)
   - ✅ Public
   - ❌ НЕ добавляйте README, .gitignore, или license
   - Нажмите `Create repository`

2. **В терминале Cursor перейдите в папку проекта:**
```bash
cd путь/к/portfolio-site
```

3. **Инициализируйте Git:**
```bash
# Инициализируйте репозиторий
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Portfolio site"

# Переименуйте ветку в main (если нужно)
git branch -M main

# Подключите удаленный репозиторий (ЗАМЕНИТЕ username на ваш)
git remote add origin https://github.com/username/username.github.io.git

# Отправьте код на GitHub
git push -u origin main
```

### Вариант 2: Обычный репозиторий с проектом

Этот вариант даст URL: `https://username.github.io/portfolio`

1. **Создайте репозиторий на GitHub:**
   - Назовите его например `portfolio` или `my-website`
   - ✅ Public
   - ❌ НЕ добавляйте README
   - Нажмите `Create repository`

2. **В терминале:**
```bash
cd путь/к/portfolio-site

git init
git add .
git commit -m "Initial commit: Portfolio site"
git branch -M main

# ЗАМЕНИТЕ username и repository-name
git remote add origin https://github.com/username/repository-name.git
git push -u origin main
```

---

## 🚀 Первый коммит

### Создание .gitignore

Создайте файл `.gitignore` в корне проекта:

```
# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log

# Временные файлы
*.tmp
*~
```

### Базовые команды Git

```bash
# Проверить статус (какие файлы изменены)
git status

# Добавить все файлы для коммита
git add .

# Добавить конкретный файл
git add index.html

# Создать коммит с сообщением
git commit -m "Описание изменений"

# Отправить изменения на GitHub
git push

# Получить последние изменения с GitHub
git pull
```

---

## 🌐 Публикация на GitHub Pages

### Для специального репозитория (username.github.io)

**Сайт автоматически опубликуется после первого push!**

Ваш сайт будет доступен по адресу: `https://username.github.io`

Обычно требуется 1-2 минуты после push для первой публикации.

### Для обычного репозитория

1. **Откройте репозиторий на GitHub**
2. **Перейдите в Settings** (настройки репозитория)
3. **Найдите раздел "Pages"** в левом меню
4. **В разделе "Source":**
   - Branch: выберите `main`
   - Folder: выберите `/ (root)`
   - Нажмите `Save`

5. **Подождите 1-2 минуты**

Ваш сайт будет доступен: `https://username.github.io/repository-name`

### Проверка публикации

```bash
# Проверьте статус GitHub Actions (если настроены)
# Зайдите на GitHub → ваш репозиторий → вкладка "Actions"
```

На вкладке Settings → Pages вы увидите сообщение:
> ✅ Your site is published at https://username.github.io

---

## 🔄 Обновление сайта

### Рабочий процесс (workflow)

```bash
# 1. Внесите изменения в файлы (в Cursor)

# 2. Проверьте, что изменилось
git status

# 3. Добавьте изменения
git add .

# 4. Создайте коммит с описанием
git commit -m "Update: добавил новую статью о React"

# 5. Отправьте на GitHub
git push

# 6. Подождите 1-2 минуты - изменения появятся на сайте
```

### Хорошие практики для коммитов

**Плохо:**
```bash
git commit -m "update"
git commit -m "fix"
git commit -m "asdf"
```

**Хорошо:**
```bash
git commit -m "Add: секция с сертификатами"
git commit -m "Update: обновил описание опыта работы"
git commit -m "Fix: исправил отображение видео на мобильных"
git commit -m "Style: изменил цветовую схему на темную тему"
```

**Префиксы для коммитов:**
- `Add:` - добавление нового функционала
- `Update:` - обновление существующего контента
- `Fix:` - исправление ошибок
- `Style:` - изменения стилей (CSS)
- `Refactor:` - рефакторинг кода
- `Docs:` - обновление документации

---

## 🎯 Продвинутые возможности

### Кастомный домен

Если у вас есть свой домен (например, `yourname.com`):

1. **Создайте файл `CNAME` в корне проекта:**
```
yourname.com
```

2. **У вашего регистратора домена настройте DNS:**

Добавьте A-записи:
```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

Или CNAME запись:
```
username.github.io
```

3. **Подождите 24-48 часов для распространения DNS**

4. **На GitHub в Settings → Pages:**
   - Введите ваш домен в поле "Custom domain"
   - Нажмите Save
   - ✅ Включите "Enforce HTTPS"

### Автоматическое развёртывание (GitHub Actions)

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

Этот workflow будет автоматически публиковать изменения при каждом push.

### Работа с ветками

```bash
# Создать новую ветку для экспериментов
git checkout -b feature/new-design

# Внести изменения и закоммитить
git add .
git commit -m "Add: новый дизайн hero секции"

# Вернуться на main
git checkout main

# Слить изменения из feature ветки
git merge feature/new-design

# Отправить на GitHub
git push

# Удалить ненужную ветку
git branch -d feature/new-design
```

### Откат изменений

```bash
# Посмотреть историю коммитов
git log --oneline

# Вернуться к предыдущему коммиту (мягкий откат)
git reset --soft HEAD~1

# Жесткий откат (удалит изменения!)
git reset --hard HEAD~1

# Откатить конкретный файл
git checkout HEAD -- index.html

# Откатить к конкретному коммиту
git reset --hard abc1234
```

### GitHub Pages с поддиректорией

Если ваш сайт не в корне репозитория:

```bash
# Структура:
repository/
  ├── docs/
  │   ├── index.html
  │   ├── styles.css
  │   └── script.js
  └── README.md
```

В Settings → Pages выберите:
- Branch: `main`
- Folder: `/docs`

---

## 🔍 Troubleshooting

### Проблема: Изменения не появляются на сайте

**Решения:**

1. **Проверьте статус публикации:**
   - GitHub → репозиторий → Settings → Pages
   - Должно быть: "Your site is live at..."

2. **Очистите кеш браузера:**
   - Cmd/Ctrl + Shift + R (жесткая перезагрузка)
   - Или откройте в режиме инкогнито

3. **Проверьте Actions:**
   - GitHub → вкладка Actions
   - Последний workflow должен быть зелёным (успешным)

4. **Подождите дольше:**
   - Первая публикация может занять до 10 минут
   - Последующие обновления - 1-2 минуты

### Проблема: 404 ошибка на GitHub Pages

**Причины и решения:**

1. **Файл index.html должен быть в корне** (или в выбранной папке)
2. **Репозиторий должен быть Public**
3. **Проверьте настройки в Settings → Pages**
4. **Убедитесь, что push прошел успешно:**
```bash
git status  # Должно быть: nothing to commit, working tree clean
```

### Проблема: Ошибка при push

**Ошибка:** `! [rejected] main -> main (fetch first)`

**Решение:**
```bash
git pull --rebase
git push
```

**Ошибка:** `Permission denied (publickey)`

**Решение:**
```bash
# Используйте HTTPS вместо SSH
git remote set-url origin https://github.com/username/repository.git

# Или настройте SSH ключи (рекомендуется):
# https://docs.github.com/en/authentication/connecting-to-github-with-ssh
```

### Проблема: Сайт показывает старую версию

**Решения:**

1. **Жесткая перезагрузка:**
```
Cmd/Ctrl + Shift + R
```

2. **Проверьте, что изменения залиты:**
```bash
git log --oneline -n 5  # Последние 5 коммитов
```

3. **Добавьте параметр к URL:**
```
https://username.github.io?v=2
```

4. **Проверьте, нет ли кеширующего сервиса:**
   - Cloudflare
   - Другие CDN

---

## 📊 Мониторинг и аналитика

### Google Analytics

Добавьте в `<head>` в `index.html`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

### GitHub Insights

На странице репозитория:
- **Insights → Traffic** - просмотры и клоны
- **Insights → Visitors** - уникальные посетители

---

## 🔒 Безопасность

### Что НЕ стоит публиковать в Git

❌ **Никогда не добавляйте:**
- API ключи
- Пароли
- Секретные токены
- Персональные данные
- Большие файлы (>100MB)

✅ **Используйте .gitignore:**
```
# Секреты
.env
secrets.json
config.private.js

# Большие файлы
*.mp4
*.zip
large-datasets/
```

### Удаление конфиденциальных данных из истории

Если случайно закоммитили секреты:

```bash
# ОПАСНО: переписывает историю!
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch secrets.json" \
  --prune-empty --tag-name-filter cat -- --all

# Отправьте изменения
git push origin --force --all
```

**Лучше:** сразу смените скомпрометированные ключи/пароли!

---

## 📚 Полезные команды Git

### Информация

```bash
# Статус репозитория
git status

# История коммитов
git log
git log --oneline
git log --graph --all

# Информация о удаленных репозиториях
git remote -v

# Различия между файлами
git diff
git diff index.html
```

### Управление файлами

```bash
# Добавить файл
git add filename

# Добавить все файлы
git add .

# Удалить файл из Git (но оставить локально)
git rm --cached filename

# Переименовать файл
git mv oldname newname
```

### Отмена изменений

```bash
# Отменить изменения в файле (до add)
git checkout -- filename

# Убрать файл из staging (после add)
git reset HEAD filename

# Изменить последний коммит
git commit --amend -m "Новое сообщение"
```

---

## ✅ Checklist: Готовность к публикации

Перед первым `git push` проверьте:

- [ ] Все личные данные заполнены (имя, контакты, опыт)
- [ ] Видео добавлено и работает
- [ ] Все ссылки ведут на правильные страницы
- [ ] Сайт протестирован локально
- [ ] Нет конфиденциальных данных в коде
- [ ] `.gitignore` настроен правильно
- [ ] Репозиторий создан на GitHub
- [ ] Git настроен (user.name и user.email)
- [ ] README.md добавлен (опционально)

После публикации:

- [ ] Сайт открывается по URL
- [ ] Все секции отображаются корректно
- [ ] Работает на мобильных устройствах
- [ ] Проверены все ссылки
- [ ] Добавлена аналитика (опционально)

---

## 🎓 Дополнительное обучение

### Рекомендуемые ресурсы:

**Интерактивные туториалы:**
- [Learn Git Branching](https://learngitbranching.js.org/?locale=ru_RU) - визуальная игра
- [GitHub Skills](https://skills.github.com/) - официальные курсы

**Документация:**
- [Git Book](https://git-scm.com/book/ru/v2) - полное руководство на русском
- [GitHub Docs](https://docs.github.com/ru) - документация GitHub

**Видео:**
- [Git для начинающих](https://www.youtube.com/watch?v=zZBiln_2FhM) - Hexlet
- [GitHub для начинающих](https://www.youtube.com/watch?v=pUT3mx1ZRUМ) - freeCodeCamp

---

## 🚀 Следующие шаги

После успешной публикации:

1. **Добавьте ссылку в LinkedIn:**
   - Профиль → Контактная информация → Веб-сайт
   - Вставьте URL: `https://username.github.io`

2. **Обновите резюме:**
   - Добавьте ссылку на портфолио
   - Упомяните технологии: HTML, CSS, JavaScript, Git

3. **Поделитесь:**
   - Соцсети
   - Профессиональные сообщества
   - С потенциальными работодателями

4. **Поддерживайте актуальность:**
   - Регулярно добавляйте новые проекты
   - Обновляйте опыт работы
   - Добавляйте статьи и достижения

---

## 💬 Получение помощи

**При проблемах с Git:**
```bash
git --help
git commit --help
```

**Сообщество:**
- [Stack Overflow (Git)](https://stackoverflow.com/questions/tagged/git)
- [GitHub Community](https://github.community/)
- [Reddit r/git](https://reddit.com/r/git)

**Официальная поддержка:**
- [GitHub Support](https://support.github.com/)
- [Git Documentation](https://git-scm.com/doc)

---

**Поздравляю! Ваш сайт-портфолио готов к публикации! 🎉**

Не забудьте добавить ссылку на ваш сайт в LinkedIn и резюме!
