# 🌐 Language Switcher Documentation

## Обзор

Переключатель языка реализован в стиле **Minimal Pills** (вариант D) и полностью интегрирован в портфолио.

## ✅ Реализованные функции

1. **Дизайн**: Minimal Pills (RU / EN) с плавными анимациями
2. **Автоопределение языка**: Определяет язык браузера при первом заходе
3. **Сохранение выбора**: Использует localStorage для сохранения предпочтений
4. **Мгновенное переключение**: Без перезагрузки страницы с fade-анимацией
5. **SEO**: Обновляет `lang` атрибут в `<html>`
6. **Keyboard shortcut**: `Alt+L` для быстрого переключения
7. **Адаптивность**: Полностью адаптивен для мобильных устройств

## 📁 Структура файлов

### HTML (`index.html`)
- Переключатель добавлен в header перед контактами
- Элементы с переводом имеют атрибут `data-lang-key`

### CSS (`styles.css`)
- Стили для `.language-switcher` и `.lang-btn`
- Адаптивные стили для мобильных устройств

### JavaScript (`script.js`)
- Класс `LanguageSwitcher` с полной логикой
- Объект `translations` со всеми переводами

## 🔧 Как добавить новые переводы

### Шаг 1: Добавить data-lang-key в HTML
```html
<h2 data-lang-key="my-title">Заголовок</h2>
```

### Шаг 2: Добавить перевод в объект translations
```javascript
const translations = {
    "my-title": {
        ru: "Заголовок",
        en: "Title"
    },
    // ... остальные переводы
};
```

## 📝 Текущие переводы

Переведены следующие элементы:
- Навигационное меню (О себе, Опыт, Case Studies, Проекты, Контакты)
- Hero секция (заголовок, подзаголовок, кнопки, статистика)
- Заголовки секций (О себе, Опыт работы, Case Studies, GitHub проекты, Контакты)
- Подзаголовок Case Studies

## 🎨 Стилизация

Переключатель использует:
- Полупрозрачный фон с blur эффектом
- Белый текст на прозрачном фоне
- Активная кнопка: белый фон с цветом primary
- Плавные переходы и hover эффекты
- Адаптивный размер для мобильных

## ⌨️ Горячие клавиши

- `Alt+L` - переключение между русским и английским

## 🔄 Как это работает

1. При загрузке страницы:
   - Проверяется localStorage на наличие сохраненного языка
   - Если нет - определяется язык браузера
   - Если браузер на русском - выбирается RU, иначе EN

2. При переключении:
   - Обновляется localStorage
   - Обновляется `lang` атрибут в `<html>`
   - Все элементы с `data-lang-key` обновляются с fade-анимацией
   - Активная кнопка подсвечивается

## 📱 Мобильная версия

На мобильных устройствах:
- Переключатель уменьшается в размере
- Сохраняет все функции
- Располагается в header рядом с hamburger меню

## 🚀 Интеграция с GitHub Pages

Переключатель работает полностью на клиентской стороне:
- Не требует backend
- Не требует API
- Работает сразу после загрузки файлов
- Совместим со всеми современными браузерами

## 💡 Примеры использования

### Добавление перевода для нового элемента

**HTML:**
```html
<p data-lang-key="about-description">Описание о себе</p>
```

**JavaScript:**
```javascript
"about-description": {
    ru: "Описание о себе",
    en: "About me description"
}
```

### Добавление перевода для кейсов (если нужно)

Для больших блоков текста можно использовать структуру с `data-lang`:

```html
<div data-lang="ru">
    <!-- Русский контент -->
</div>
<div data-lang="en" style="display: none;">
    <!-- English content -->
</div>
```

JavaScript автоматически покажет/скроет нужную версию при переключении языка.

## 🐛 Отладка

Если переключатель не работает:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что `script.js` подключен в `index.html`
3. Проверьте, что элементы имеют правильные `data-lang-key` атрибуты
4. Убедитесь, что переводы добавлены в объект `translations`

## 📊 Производительность

- Минимальный JavaScript (без внешних библиотек)
- Быстрое переключение без перезагрузки
- Оптимизированные CSS transitions
- Легкий вес (добавлено ~3KB кода)

---

**Готово к использованию!** 🎉

Переключатель полностью функционален и готов к работе на GitHub Pages.

